# 某讯滑块自动化问题排查与历史问题

这份文档记录这个项目在实际跑通过程中遇到过的典型问题。

目标不是只告诉接手者“最后代码长什么样”，而是说明：

1. 当时出了什么问题
2. 为什么会出这个问题
3. 最后是怎么解决的
4. 以后如果再遇到，应该先看哪里

---

## 1. 快速索引

如果你现在遇到的是下面这些现象，可以直接跳到对应章节：

- 缺口明显存在，但算法匹配不到，或者 `final_x` 偏得很离谱
  看第 2 节

- 检测看起来差不多，但浏览器拖完还是 `errorCode=50`
  看第 3 节

- 账号密码已经填进去了，但“记住登录状态”点不上
  看第 4 节

- “我已阅读并同意协议”点不上
  看第 5 节

- 登录按钮看得见，但代码点不到
  看第 6 节

- 自动化没有进入正确的登录表单
  看第 7 节

---

## 2. 问题一：缺口匹配不到，或者 `final_x` 偏移很大

### 现象

最早遇到的核心问题就是：

- 脚本能抓到背景图和精灵图
- 也能正常运行检测函数
- 但算出来的缺口位置明显不对
- 有时会出现大偏移，甚至方向都错

一次典型日志如下：

```text
[检测] final_x=10 subpixel_x=9.96 match_y=152 conf=0.212 coarse=(10,152)/0.212
[drive] 拖动距离 = (fx10 - ix50) * scale0.5060 * ratio1.0 = -20.2px
[capture] VERIFY 提交! 成功=False
[verify] errorCode=50
```

这组日志说明：

- 初始拼图块 `init_x=50`
- 算法却认为目标缺口在 `x=10`
- 导致拖动距离是负数
- 浏览器提交后直接验证失败

### 原因

当时的主要原因不是拖动，而是**检测算法本身不稳定**。

原来的思路更偏向：

- 用 OpenCV `matchTemplate`
- 做拼图块或边缘的相关匹配

这种方法在普通图片上没问题，但某讯滑块这里有几个特殊点：

1. 背景纹理比较复杂  
   很多区域本身就有强边缘、强对比度，容易出现假峰值。

2. 缺口不一定只是“边缘轮廓像”  
   实际上很多样本里，缺口区域是一个明显更暗的“洞位”。

3. 单纯边缘相关容易被背景误导  
   算法会把某块纹理当成目标，而不是真正的缺口。

4. `y` 方向如果搜索范围太宽，也会放大误匹配  
   即使 `x` 大体正确，`y` 跑偏后也会让模板分数失真。

### 解决方案

最后不是继续微调旧模板匹配参数，而是把检测逻辑换成了：

- **深色洞位优先**
- **边缘匹配辅助**

现在的实现位置：

- `D:\工作区文件夹\滑块\某讯滑块自动化\detection_core.py`
- 核心函数：`detect_gap_position(...)`

具体做法：

1. 从精灵图中裁出真实拼图块
2. 使用 alpha 通道生成有效区域掩码
3. 计算拼图块内部区域的亮度均值
4. 计算拼图块外圈 ring 区域的亮度均值
5. 用“外圈比内部更亮、内部更暗”的特征判断缺口位置
6. 再把边缘匹配分数作为辅助评分
7. 只在 `init_y ± y_band` 范围内搜索
8. 对最终 `x` 做亚像素修正

简化理解就是：

- 原来是在找“长得像拼图轮廓的位置”
- 现在是在找“看起来像一个暗洞的位置”，再用边缘去校正

### 为什么这样改有效

因为这个验证码场景里，真正稳定的信号往往不是轮廓，而是：

- 缺口内部发暗
- 外圈相对更亮
- 缺口大致出现在和初始块同一水平带附近

这个信息比纯边缘相关更抗噪。

### 怎么验证

优先看两类证据：

1. 运行主流程后，看控制台日志

重点关注：

- `final_x`
- `subpixel_x`
- `match_y`
- `confidence`
- `hole_score`
- `edge_score`

2. 对照真实提交值

可以运行：

```powershell
python D:\工作区文件夹\滑块\某讯滑块自动化\capture_verify.py
```

它的作用是把：

- 检测出来的 `final_x`
- 浏览器真实提交的 `ans`

做对照，判断偏差是否收敛。

### 以后再遇到时先检查什么

优先按这个顺序排查：

1. `downloaded_images/live_bg.png` 和 `downloaded_images/live_sprite.png` 是否抓对了
2. `prehandle.json` 里的 `fg_elem_list` 是否还是当前这一套结构
3. `piece["sprite_pos"]`、`piece["size_2d"]`、`piece["init_pos"]` 是否正常
4. `bg_size2d` 是否仍然是设计坐标系 `[672, 480]`
5. `y_band` 是否过小或过大
6. 当前验证码样式是否已经变化，不再是“暗洞型”

---

## 3. 问题二：检测大致正常，但拖动后还是 `errorCode=50`

### 现象

有一种情况是：

- 算法并没有明显跑飞
- 视觉上滑块也拖到了差不多的位置
- 但最终还是验证失败

### 原因

这类问题通常不是“检测完全错了”，而是下面几个因素之一：

1. 设计坐标系和页面显示坐标系不一致
2. 背景图显示宽度和原始设计宽度存在缩放
3. 拖动把手位置取错了
4. `DRAG_RATIO` 需要微调
5. 页面上取到的不是实际可拖动的滑块元素

### 解决方案

当前主流程里的处理方式在：

- `D:\工作区文件夹\滑块\某讯滑块自动化\tdc_solver.py`
- 核心函数：`solve(page)`、`drive_slider(...)`

关键公式是：

```python
distance_px = (fx - ix) * scale * DRAG_RATIO
```

其中：

- `fx` 是检测出的目标位置
- `ix` 是拼图块初始位置
- `scale` 是页面显示宽度与设计宽度的比例
- `DRAG_RATIO` 是人工补偿系数

这次能跑通的关键点有两个：

1. 不再直接把设计坐标当成浏览器像素拖动
2. 用页面实际显示的背景宽度去算 `scale`

### 怎么验证

运行主流程时，看这段日志：

```text
[drive] iframe=...
[drive] 背景#slideBg显示宽=340 → scale=0.5060
[drive] 把手... 中心=(...)
[drive] 拖动距离 = (fx... - ix...) * scale... * ratio... = ...px
```

如果这里的 `scale` 和显示宽度明显不合理，优先查页面元素是否变了。

### 以后再遇到时先检查什么

1. 背景元素选择器是否还能拿到真实宽度
2. 把手元素选择器是否还是当前可拖动元素
3. `DRAG_RATIO` 是否需要从 `1.0` 微调
4. 识别出的 `final_x` 是否已经明显错位，如果是，先回去查检测器，不要先调拖动

---

## 4. 问题三：“记住登录状态”勾选不上

### 现象

自动化已经把账号密码填进去了，但“记住登录状态”一直没有被真正勾选。

表面上像是点了，实际上状态没变。

### 原因

这类 Element UI 风格的 checkbox，经常有两个坑：

1. 真正可点击的不是 `input`
2. 页面上可见的点击区域是 `span.el-checkbox__inner`

也就是说：

- `input.el-checkbox__original` 更像状态载体
- `span.el-checkbox__inner` 才是实际交互层

如果只对 `input` 做普通点击或填值，常常不会生效。

### 解决方案

当前代码位置：

- `D:\工作区文件夹\滑块\某讯滑块自动化\tdc_solver.py`
- 相关函数：
  - `safe_is_checked(locator)`
  - `ensure_checked(locator, label)`
  - `ensure_checkbox_by_text(page, text, label)`
  - `ensure_checkbox_by_index(page, index, label)`

最终采用的是多层兜底策略：

1. 优先按 `label.el-checkbox` 的索引定位
2. 点击其内部 `span.el-checkbox__inner`
3. 再检查 `input[type='checkbox']` 是否已变成 checked
4. 如果索引方式失效，再尝试按文本匹配
5. 最后才退回原始 checkbox input 方案

当前实际稳定的经验是：

- 第 0 个 checkbox 视为“记住登录状态”
- 第 1 个 checkbox 视为“同意协议”

### 怎么验证

看运行日志：

- `[login] 已勾选: 记住登录状态`
- 或 `[login] 已是勾选状态: 记住登录状态`

如果没有出现这类日志，说明这一层逻辑可能没有命中。

### 以后再遇到时先检查什么

1. 页面 DOM 顺序是否变了，导致“第 0 个 checkbox”不再是记住登录
2. `label.el-checkbox` 是否还存在
3. `span.el-checkbox__inner` 是否仍是实际点击区域
4. 页面是否加入了遮罩、禁用态或异步渲染延迟

---

## 5. 问题四：“同意协议”勾选不上

### 现象

“记住登录状态”能点，但“我已阅读并同意协议”没点中，导致登录按钮后续逻辑失败。

### 原因

根本原因和上一个问题类似，也是 checkbox 结构与普通 input 不同。

另外，这个协议勾选框还有一个额外问题：

- 它可能已经是 `is-checked`
- 也可能和记住登录的 DOM 非常像
- 单靠类名不容易区分

### 解决方案

最后采用的是“位置优先，文本兜底”的策略：

1. 先按第 1 个 `label.el-checkbox` 处理
2. 不行再按文本 `我已阅读并同意`
3. 再不行才尝试直接处理 checkbox input

这样做的原因是：

- 文本匹配更稳，但有时渲染结构特殊
- 索引匹配更直接，但前提是页面结构没大改
- 两者组合能提高稳定性

### 怎么验证

看日志：

- `[login] 已勾选: 同意协议`
- 或 `[login] 已是勾选状态: 同意协议`

### 以后再遇到时先检查什么

1. 这个复选框是否默认就已经 checked
2. 是否新增了其他 checkbox，导致索引偏移
3. 文本是否改了，不再包含“我已阅读并同意”

---

## 6. 问题五：登录按钮点不到

### 现象

前面的账号、密码、checkbox 都正常，但自动化没有真正触发登录。

### 原因

开始时容易默认把“登录”当成：

- `<button>`
- 或标准表单提交按钮

但这个页面实际给出的结构是：

```html
<div class="submit-btn">登录</div>
```

也就是说它不是标准 button，而是一个 `div`。

如果代码只写了：

- `get_by_role("button", name="登录")`
- 或 `locator("button")`

那就可能根本匹配不到。

### 解决方案

当前代码里已经把登录按钮的优先匹配顺序改成：

1. `div.submit-btn`
2. `button`
3. `.login-btn, .btn-login`

位置：

- `D:\工作区文件夹\滑块\某讯滑块自动化\tdc_solver.py`
- 函数：`auto_login(page)`

### 怎么验证

看日志：

- `[login] 已点击登录，等待滑块出现…`

如果日志有这句，但页面仍然没反应，说明不是选择器问题，而是：

- 页面校验没通过
- 某个 checkbox 其实没成功
- 或页面有额外前置条件

---

## 7. 问题六：自动化没有进入正确的登录模式

### 现象

脚本启动后虽然打开了登录页，但输入框不对，或者填值逻辑跑在错误表单上。

### 原因

这个页面并不一定默认就是“密码登录”。

如果停留在：

- 验证码登录
- 其他 tab

那脚本虽然能找到 input，但不一定是正确的账号密码输入框。

### 解决方案

在 `auto_login(page)` 一开始就先尝试切到：

- `密码登录`

当前做法：

1. 先找文字 `密码登录`
2. 再找 `.tabs .pane` 中包含 `密码登录` 的元素
3. 点击后再去填账号密码

### 怎么验证

看日志：

- `[login] 已切换到密码登录`

如果没有这句，也不一定是错误，因为代码里做了异常兜底；
但如果后续输入框经常取错，就要优先检查这一步。

---

## 8. 一次真正的定位过程总结

这次项目能跑通，不是只改了一个参数，而是分了两层处理：

### 第一层：先确认是不是“识别错了”

核心判断依据：

- `final_x` 是否离谱
- 拖动距离是否出现明显负数或过大
- `capture_verify.py` 对比出来的误差是否很大

在这次案例里，最关键的错误信号就是：

- `final_x=10`
- 初始位置 `init_x=50`
- 说明不是“差几像素”，而是“候选点都找错了”

所以当时不应该先调拖动，而应该先重做检测器。

### 第二层：识别修好后，再检查页面自动化

识别稳定之后，问题就集中到前端交互：

1. checkbox 点不中
2. 登录按钮不是原生 button
3. 页面要先切密码登录

这几类问题的共同点是：

- 视觉上页面很简单
- 但 DOM 结构并不是最直观的那种写法

所以真正可靠的做法不是“猜控件类型”，而是：

- 先看实际 HTML
- 再决定点击谁

---

## 9. 后续维护建议

为了减少以后重复踩坑，建议继续做这几件事：

### 1. 把登录页选择器配置化

建议单独维护：

- 账号输入框候选
- 密码输入框候选
- checkbox 候选
- 登录按钮候选

这样页面变更时，不需要改主流程逻辑。

### 2. 保留一次成功样本

建议保留这些文件作为基准：

- `capture/prehandle.json`
- `capture/verify_req.txt`
- `capture/verify_resp.json`
- `downloaded_images/live_bg.png`
- `downloaded_images/live_sprite.png`

这样新版本失效时，可以立刻判断：

- 是图片变了
- 是接口结构变了
- 还是页面交互变了

### 3. 修改时优先判断是哪一层出问题

建议排查顺序固定成：

1. 登录页元素问题
2. 验证码资源抓取问题
3. 缺口检测问题
4. 缩放和拖动问题
5. 最终 verify 请求问题

不要一开始就盲调 `DRAG_RATIO`。

---

## 10. 结论

这次最关键的历史经验有三条：

1. 缺口识别偏移大时，往往不是“差几像素”，而是检测思路错了  
   单纯模板匹配不稳，后来改成了“深色洞位优先 + 边缘辅助”。

2. 浏览器自动化失败时，不要只看视觉，要看真实 DOM  
   checkbox 真正该点的是 `span.el-checkbox__inner`，登录按钮实际是 `div.submit-btn`。

3. 先分层定位，再动代码  
   先判断是登录问题、抓图问题、检测问题，还是拖动问题，效率会高很多。
