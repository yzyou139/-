# 某讯滑块自动化环境配置说明

这份文档用于说明这个项目在一台新电脑上如何完成环境安装。

适用对象：

- 第一次接手项目的人
- 需要把项目打包给别人使用的人
- 需要在新电脑上重新部署的人

---

## 1. 项目运行依赖

这套代码当前依赖以下内容：

### Python 版本

推荐：

- `Python 3.10`
- `Python 3.11`
- `Python 3.12`

不建议使用太老的 Python 版本。

### Python 第三方包

项目当前实际使用到的依赖只有这几个：

- `playwright`
- `opencv-python`
- `numpy`

项目目录里已经补了依赖文件：

- `requirements.txt`

### 浏览器依赖

当前脚本使用的是：

```python
channel="chrome"
```

这意味着：

- 目标电脑上需要安装 `Google Chrome`

如果电脑没有安装 Chrome，脚本启动浏览器时可能会失败。

### 操作系统建议

推荐环境：

- `Windows 10`
- `Windows 11`

因为当前项目实际开发和验证是在 Windows 下完成的。

---

## 2. 建议的目录结构

建议至少保留这些文件：

- `tdc_solver.py`
- `detection_core.py`
- `capture_verify.py`
- `requirements.txt`
- `README.md`
- `开发接手说明.md`
- `问题排查与历史问题.md`
- `环境配置说明.md`

运行产物目录如果没有也没关系，脚本首次运行时会自动创建：

- `capture/`
- `downloaded_images/`
- `detection_results/`
- `chrome_profile/`

---

## 3. 从零安装步骤

下面是一套最稳的安装流程。

### 第一步：安装 Python

先确认电脑上已经安装 Python。

在终端执行：

```powershell
python --version
```

如果能看到类似输出：

```text
Python 3.11.9
```

说明 Python 已安装。

如果提示找不到 `python`，先安装 Python，再继续后面的步骤。

---

### 第二步：进入项目目录

假设项目放在：

```text
D:\项目\某讯滑块自动化
```

那么先进入目录：

```powershell
cd D:\项目\某讯滑块自动化
```

以后所有命令都建议在这个目录里执行。

---

### 第三步：创建虚拟环境

建议使用虚拟环境，不要直接把依赖装到系统 Python 里。

```powershell
python -m venv .venv
```

创建完成后，激活虚拟环境：

```powershell
.\.venv\Scripts\activate
```

激活成功后，命令行前面通常会出现：

```text
(.venv)
```

---

### 第四步：安装 Python 依赖

执行：

```powershell
pip install -r requirements.txt
```

如果安装成功，就说明这几个核心库已经就绪：

- `playwright`
- `opencv-python`
- `numpy`

---

### 第五步：确认 Chrome 已安装

因为项目当前代码指定了：

```python
channel="chrome"
```

所以请确认电脑上安装了 `Google Chrome` 浏览器。

如果没有 Chrome，建议先安装 Chrome，再运行脚本。

---

## 4. 运行方式

### 主流程运行

在项目目录下执行：

```powershell
python tdc_solver.py
```

这个脚本负责：

1. 打开登录页
2. 自动填写账号密码
3. 自动勾选登录相关复选框
4. 自动点击登录
5. 等待某讯滑块出现
6. 自动识别缺口并拖动
7. 抓取 verify 请求和响应

### 检测验证脚本

在项目目录下执行：

```powershell
python capture_verify.py
```

这个脚本用于：

- 对比检测结果和真实提交值
- 判断当前缺口识别误差是否正常

---

## 5. 当前代码里的可配置项

当前主脚本中的几个关键配置在：

- `tdc_solver.py`

主要包括：

### 登录地址

```python
LOGIN_URL = "https://www.eeo.cn/cn/login"
```

### 最长等待时间

```python
WAIT_SECONDS = 300
```

### 拖动补偿比例

```python
DRAG_RATIO = 1.0
```

### 测试账号和密码

```python
TEST_USERNAME = "12345678901"
TEST_PASSWORD = "12345678"
```

如果要正式使用，建议把账号密码改成：

- 配置文件读取
  或
- 环境变量读取

不要长期写死在代码里。

---

## 6. 常见安装问题

### 问题 1：`python` 命令找不到

说明 Python 没装好，或者没有加入系统环境变量。

处理方式：

- 重新安装 Python
- 安装时勾选“Add Python to PATH”

---

### 问题 2：`pip install -r requirements.txt` 失败

常见原因：

- 网络问题
- Python 版本不兼容
- 没激活虚拟环境

建议先检查：

```powershell
python --version
pip --version
```

---

### 问题 3：浏览器启动失败

如果报错和 `chrome`、`channel="chrome"` 有关，通常说明：

- 电脑没有安装 Google Chrome
- 或 Playwright 无法找到本机 Chrome

解决思路：

1. 先安装 `Google Chrome`
2. 再重新运行脚本

如果后续要做成“别人机器更容易跑”的版本，可以把：

```python
channel="chrome"
```

改成默认 Chromium 方案，但那属于后续改造，不是当前这份代码的默认行为。

---

### 问题 4：依赖装好了，但运行后页面行为不对

这通常不是环境问题，而是：

- 登录页 DOM 变了
- checkbox 结构变了
- 登录按钮结构变了
- 滑块样式变了

这时优先看：

- `开发接手说明.md`
- `问题排查与历史问题.md`

---

## 7. 最简交付说明

如果你要把这个项目发给别人，可以直接把下面这段话一起发过去：

```text
1. 安装 Python 3.10+ 和 Google Chrome
2. 进入项目目录
3. 执行 python -m venv .venv
4. 执行 .\.venv\Scripts\activate
5. 执行 pip install -r requirements.txt
6. 执行 python tdc_solver.py
```

---

## 8. 结论

这个项目当前的环境要求并不复杂，核心只有两类：

1. Python 依赖：
   `playwright`、`opencv-python`、`numpy`

2. 运行前置：
   本机安装 `Google Chrome`

只要这两部分满足，其他目录和运行产物脚本都会自动处理。
